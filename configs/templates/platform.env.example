# Core platform environment variables for Shristyverse LLM Suite modules.
# Duplicate into your module as `.env` and customize per your deployment plan.

# Primary API credentials (request from platform-ops@shristyverse.com)
SHRISTYVERSE_PLATFORM_KEY=replace-with-issued-key

# Default model endpoints (switch to internal hosts when available)
SHRISTYVERSE_MODEL_ENDPOINT=https://models.shristyverse.com/v1

# Observability routing (logs, traces, metrics)
SHRISTYVERSE_TELEMETRY_ENDPOINT=https://telemetry.shristyverse.com/ingest

# Optional: feature flag service endpoint
SHRISTYVERSE_FEATURE_FLAGS_URL=https://flags.shristyverse.com/api
