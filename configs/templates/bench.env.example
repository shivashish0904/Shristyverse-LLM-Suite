# Bench harness environment variables
# Copy this template to <module>/.env and fill with Shristyverse-issued credentials.

# Required for authenticated API calls during evaluation
SHRISTYVERSE_API_KEY=replace-with-service-key

# Optional: override default region or deployment target
SHRISTYVERSE_REGION=tokyo-1

# Logging bucket for benchmark artifacts
SHRISTYVERSE_BENCH_BUCKET=s3://shristyverse-bench-artifacts
